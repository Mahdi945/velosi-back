# ===============================================
# 🔒 FICHIER DE CONFIGURATION - EXEMPLE
# ===============================================
# 
# ⚠️ CE FICHIER NE CONTIENT PAS DE VRAIES CREDENTIALS
# ⚠️ Copier ce fichier en .env et remplir avec vos vraies valeurs
# ⚠️ Le fichier .env est dans .gitignore et ne doit JAMAIS être commité
#
# Instructions:
# 1. cp .env.example .env
# 2. Remplir les valeurs réelles dans .env
# 3. Ne JAMAIS commiter le fichier .env
# ===============================================

# ===============================================
# BASE DE DONNÉES
# ===============================================
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=VOTRE_MOT_DE_PASSE_ICI
DB_DATABASE=velosi_db

# ===============================================
# SMTP / EMAIL - CONFIGURATION COMPLÈTE
# ===============================================
# ⚠️ IMPORTANT: Ces credentials ne doivent JAMAIS être dans le code
# 📚 Guide complet : docs/CONFIGURATION_SMTP.md
# 🧪 Test : npx ts-node scripts/test-smtp.ts

# Serveur SMTP
# Gmail: smtp.gmail.com
# Outlook: smtp-mail.outlook.com  
# SendGrid: smtp.sendgrid.net (Recommandé pour production)
# Mailgun: smtp.mailgun.org
SMTP_HOST=smtp.gmail.com

# Port SMTP
# Gmail/Outlook: 587 (TLS) ou 465 (SSL)
# SendGrid/Mailgun: 587
SMTP_PORT=587

# Sécurité TLS (true pour port 465, false pour port 587)
SMTP_SECURE=false

# Identifiants SMTP
# ⚠️ Gmail : utilisez un "Mot de passe d'application" (App Password)
#    Guide : https://support.google.com/accounts/answer/185833
# ⚠️ Outlook : utilisez votre mot de passe normal (ou App Password si 2FA activé)
SMTP_USER=votre_email@gmail.com
SMTP_PASSWORD=votre_mot_de_passe_application_gmail

# Email expéditeur (FROM) - Optionnel, par défaut = SMTP_USER
SMTP_FROM_EMAIL=noreply@velosi.com

# Nom de l'expéditeur - Optionnel, par défaut = "Velosi ERP"
SMTP_FROM_NAME=Velosi ERP

# ===============================================
# JWT / AUTHENTIFICATION
# ===============================================
JWT_SECRET=GENERER_UNE_CLE_SECRETE_ALEATOIRE_ICI_64_CARACTERES_MIN

# Durée de validité du token JWT (access token)
# Format: nombre + unité (s=secondes, m=minutes, h=heures, d=jours)
# Exemples: 3600 (secondes), 1h, 8h, 24h
# Recommandation production: 8h maximum pour la sécurité
JWT_EXPIRES_IN=8h

# Durée de validité du refresh token (pour renouveler le token)
# Recommandation: 7 à 30 jours
JWT_REFRESH_EXPIRES_IN=7d

# Expiration en secondes (pour compatibilité - DEPRECATED, utiliser JWT_EXPIRES_IN)
JWT_EXPIRATION=28800

# ===============================================
# KEYCLOAK (si utilisé)
# ===============================================
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=velosi
KEYCLOAK_CLIENT_ID=velosi-erp
KEYCLOAK_CLIENT_SECRET=VOTRE_CLIENT_SECRET_ICI

# ===============================================
# APPLICATION
# ===============================================
NODE_ENV=development
PORT=3000
CORS_ORIGIN=http://localhost:4200

# ===============================================
# UPLOADS / FICHIERS
# ===============================================
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# ===============================================
# LOGS
# ===============================================
LOG_LEVEL=debug

# ===============================================
# 📝 NOTES DE SÉCURITÉ
# ===============================================
# 
# 1. Ne JAMAIS commiter ce fichier avec des vraies credentials
# 2. Utiliser des mots de passe forts (min 16 caractères)
# 3. Changer tous les secrets en production
# 4. Utiliser un gestionnaire de secrets en production (AWS Secrets Manager, Azure Key Vault, etc.)
# 5. Activer la double authentification sur Gmail si utilisé
# 
# Génération de secrets sécurisés:
# - Linux/Mac: openssl rand -base64 64
# - Windows PowerShell: [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Maximum 256 }))
# - Node.js: require('crypto').randomBytes(64).toString('base64')
